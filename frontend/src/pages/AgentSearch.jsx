export default function AgentSearch() {
    return (
      <div>
        <h2>Find Customer</h2>
  
        <input placeholder="Search by name / id / application id" />
        <button>Search</button>
  
        <p>Sort ascending / descending  
           Filter by gender, policy</p>
      </div>
    );
  }
  